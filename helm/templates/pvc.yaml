apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: youtube-music-scrobbler-data
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.storageClass }}
  storageClassName: {{ .Values.storageClass | quote }}
  {{- end }}
  resources:
    requests:
      storage: 5Mi
